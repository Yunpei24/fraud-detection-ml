# Python Dependencies for Data Module
# Fraud Detection System - Data Ingestion, Validation, and Processing
# Optimized for Python 3.11
# NOTE: azure-eventhub requires cmake compilation which can fail on macOS
# Install with: pip install -r requirements.txt
# For Azure Event Hub support, use: pip install -r requirements-azure.txt

# Core Data Processing
# Pinned versions compatible with Python 3.11
pandas==2.1.4
numpy==1.26.3
scipy==1.12.0

# Cloud & Storage (Azure SDK - lightweight versions)
# Note: azure-eventhub removed due to uamqp compilation issues on macOS
# Use requirements-azure.txt if you need Event Hub support
azure-storage-blob==12.18.3
azure-identity==1.14.0
azure-core==1.28.0

# Database & SQL
sqlalchemy==2.0.23
pyodbc==5.1.0

# Streaming & Message Queues (Kafka instead of Event Hub by default)
kafka-python==2.0.2
redis==5.0.1

# Data Validation & Processing
pydantic==2.5.3
pydantic-settings==2.1.0  # For settings management
marshmallow==3.20.1

# Monitoring & Logging
prometheus-client==0.19.0
python-json-logger==2.0.7

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-asyncio==0.23.2
faker==22.0.0

# Data Quality
jsonschema==4.20.0

# Feature Engineering
scikit-learn==1.3.2

# Utilities
python-dotenv==1.0.0
requests==2.31.0
tenacity==8.2.3  # Retry logic
typing-extensions==4.9.0  # For compatibility

# Development
black==23.12.1
flake8==6.1.0
mypy==1.8.0
isort==5.13.2

# Documentation
sphinx==7.2.6
sphinx-rtd-theme==2.0.0

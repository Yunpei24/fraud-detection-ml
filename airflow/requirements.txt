setuptools>=70.0.0 

# Apache Airflow
apache-airflow==2.7.0
apache-airflow[postgres,celery,redis]==2.7.0

# Providers (removed Databricks, added Docker)
apache-airflow-providers-apache-spark==4.1.1
apache-airflow-providers-http==4.5.0
apache-airflow-providers-docker==3.7.3
apache-airflow-providers-postgres==5.6.0

# ML & Data Science
xgboost==2.0.3
scikit-learn==1.3.2
pandas==2.1.4
numpy==1.26.2
mlflow==2.10.2

# Database
psycopg2-binary==2.9.9
# NOTE: Airflow 2.7.0 requires sqlalchemy<2.0
# Other services (API, Data, etc.) can use SQLAlchemy 2.x
sqlalchemy>=1.4.0,<2.0.0

# Azure
azure-storage-blob==12.19.0
azure-identity==1.15.0

# Cache
redis==5.0.1

# Utilities
# NOTE: Airflow 2.7.0 requires pydantic<2.0.0
# pydantic-settings v2 requires pydantic v2, so we can't use it here
pydantic>=1.10.0,<2.0.0
python-dotenv==1.0.0
structlog==24.1.0

# Testing
pytest==7.4.3
pytest-cov==4.1.0
